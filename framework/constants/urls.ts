// Central place for base URLs used across the test suite.
// Keeping URLs here (instead of hard-coded in fixtures/tests) means a single
// change updates every test if the target site/environment ever changes.
export const URLS = {
  AMAZON_HOME: 'https://www.amazon.co.uk',
};
